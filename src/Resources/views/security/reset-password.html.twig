{% extends '@User/security/base.html.twig' %}

{% block title %}{{ title|trans }} {% endblock %}

{% block titlePage %}{{title|trans}}{% endblock %}

{% block body %}
    
    {{ form_start(form) }}
    
    {{ form_widget(form) }}
    
    <button type="submit" class="btn btn-success"><i class="fa fa-save"></i> {{ 'reset_password'|trans }}</button>
    
    {{ form_end(form) }}
    
{% endblock %}
